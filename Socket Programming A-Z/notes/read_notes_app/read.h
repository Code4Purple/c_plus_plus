#include <vector>
#include <iostream>
#include <string>
#include <fstream>
using namespace std;


class read{

    public:
    read();
    ~read();

    string line;

    void await(){
        for (int i = 0; i < INT_MAX; i++){
            
        }

    }

    char readfile [100];
    fstream readfile;
    vector<string> list = {};
    
};