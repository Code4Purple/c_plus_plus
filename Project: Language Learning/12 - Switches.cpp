#include <iostream>

int main(){

    /*
        switch = alternative to using many "else if" statements
                 compare one value against matching cases
    
    */


    int month;
    std::cout << "Enter the month (1-12)";
    std::cin >> month;

    // wright if & else if statements for the rest of the code

    return 0;
}