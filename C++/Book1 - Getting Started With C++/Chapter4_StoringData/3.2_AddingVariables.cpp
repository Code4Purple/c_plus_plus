#include <iostream>

using namespace std;


int main(){

    // Declaring Variables
    int a, b;

    // Assigning Values to Variables
    a = 5;
    b = 10;
    cout << "Orginal Value of a : " << a << endl;
    cout << "Orginal Value of b : " << b << endl;
    cout << endl;

    // Addition of these two variables
    cout << "Add two Variables          : " << a + b << " (a to b)" << endl;
    cout << "Add a variable to a number : " << a + 5 << " (a to 5)" << endl;
}