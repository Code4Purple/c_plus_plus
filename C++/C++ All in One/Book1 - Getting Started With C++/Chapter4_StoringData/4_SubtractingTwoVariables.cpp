#include <iostream>

using namespace std;


int main(){

    // Declaring Variables
    int a, b;

    // Assigning Values to Variables
    a = 5;
    b = 10;
    cout << "Orginal Value of a : " << a << endl;
    cout << "Orginal Value of b : " << b << endl;
    cout << endl;

    // Subtraction of these two variables
    cout << "Subtract two Variables          : " << b - a  << " (b - a)" << endl;
    cout << "Subtract a variable to a number : " << a - 5 << " (a - 5)" << endl;
}